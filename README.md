# Environmental Health Intelligence Agent

Monitors public health and environmental data, summarizes risks with AI, and records verified alerts on-chain.

Generated by [Scaffold CRE](https://github.com/tigeragentt/scaffold-cre).

## Project structure

```
project.yaml                  ← CRE project settings (RPC endpoints)
secrets.yaml                  ← Secrets template (fill in before deploying)
.cre/template.yaml            ← CRE project metadata
workflow-environmental-health-intelligence-agent/                    ← Workflow folder
  main.ts                     ← Entry point
  workflow.ts                 ← Workflow logic (edit this)
  package.json
  tsconfig.json
  workflow.yaml               ← Deploy targets
  config/
    config.staging.json       ← Staging config values
    config.production.json    ← Production config values
```

## Prerequisites

- [Bun](https://bun.sh) >= 1.2.21
- [Chainlink CRE CLI](https://docs.chain.link/cre/reference/cli)

## 1. Install dependencies

Run from the **project root directory**:

```bash
bun install --cwd ./workflow-environmental-health-intelligence-agent
```

## 2. Fill in the TODOs

Search for `TODO` in the source files to find every placeholder.

- [ ] Set `healthAlertRegistryAddress` in `config.json` (currently "TODO_HEALTHALERTREGISTRY_ADDRESS")
- [ ] Fill in EVM function argument types and values in `workflow.ts`
- [ ] Define HTTP response types and consensus aggregation in `workflow.ts`
- [ ] Fill in HTTP POST request body in `workflow.ts`
- [ ] Update `encodeAbiParameters` to match your consumer contract's `onReport` param types

## 3. Simulate the workflow

Run from the **project root directory**:

```bash
cre workflow simulate workflow-environmental-health-intelligence-agent --target staging-settings
```

## 4. Send a real transaction on chain

On `.env`, define `CRE_ETH_PRIVATE_KEY`.
Use `--broadcast` to submit the transaction:

```bash
cre workflow simulate workflow-environmental-health-intelligence-agent --target=staging-settings --broadcast
```

## Push to GitHub

```bash
git init
git add .
git commit -m "Initial scaffold: Environmental Health Intelligence Agent"
git branch -M main
git remote add origin https://github.com/<your-username>/<your-repo>.git
git push -u origin main
```

## References

- [CRE SDK docs](https://docs.chain.link/cre)
- [Chainlink Agent Skills](https://github.com/smartcontractkit/chainlink-agent-skills)
- [CRE Templates](https://github.com/smartcontractkit/cre-templates)
